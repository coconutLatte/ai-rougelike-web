# Bug Tracker

> Auto-generated by daily assessment (`scripts/assess.sh`).
> Evolution engine checks this file before implementing — bugs get priority over features.

## Format

Each bug entry:
```
### BUG-<NNN>: <short title>
- **Severity**: critical | major | minor | cosmetic
- **Status**: open | fixed
- **Found**: <date>
- **Fixed**: <date> (if applicable)
- **Category**: gameplay | rendering | ui | balance | crash | performance
- **File**: <source file and line>
- **Reproduction**: <steps to reproduce>
- **Expected**: <what should happen>
- **Actual**: <what actually happens>
- **Suggestion**: <how to fix>
```

---

## Active Bugs

### BUG-001: Save/Load system is completely non-functional — deserialize() always returns null
- **Severity**: critical
- **Status**: open
- **Found**: 2026-07-21
- **Category**: crash
- **File**: `src/systems/SaveManager.ts:83-87`
- **Reproduction**: Start game → press S to save → refresh page → game starts fresh (save ignored)
- **Expected**: Game restores player position, stats, inventory, dungeon layout, floor number from save
- **Actual**: `SaveManager.deserialize()` is a stub that always returns `null`. `Game.start()` can never load a saved game
- **Suggestion**: Implement `deserialize()` to reconstruct dungeon from serialized tile data, rebuild entities, recompute visibility

### BUG-002: Enemy random-wander movement lacks bounds checking → crash
- **Severity**: critical
- **Status**: open
- **Found**: 2026-07-21
- **Category**: crash
- **File**: `src/systems/TurnManager.ts:73-88`
- **Reproduction**: Enemy wanders to map edge → picks direction toward edge → `dungeon.tiles[-1][y]` throws TypeError
- **Expected**: Out-of-bounds movement rejected; enemy stays put or picks different direction
- **Actual**: No bounds check on nx/ny before accessing `dungeon.tiles[ny][nx].blocksMovement`. Undefined access crashes the game
- **Suggestion**: Add `if (nx < 0 || nx >= dungeon.width || ny < 0 || ny >= dungeon.height) continue;` before tile access

### BUG-003: Player appears inside wall after descending floors
- **Severity**: critical
- **Status**: open
- **Found**: 2026-07-21
- **Category**: gameplay
- **File**: `src/core/Game.ts:227-250`
- **Reproduction**: Descend stairs → new floor has different dungeon layout → player position unchanged from old floor stairs
- **Expected**: Player appears in first room of new floor
- **Actual**: `newState.player = this.state.player` carries over old position. Since dungeon layout differs, position may be a Wall. Player stuck
- **Suggestion**: Reset player position to `newState.dungeon.rooms[0]` center after `createNewGame()`

### BUG-004: Only 3 weakest enemy types ever spawn (Skeleton/Orc are dead code)
- **Severity**: major
- **Status**: open
- **Found**: 2026-07-21
- **Category**: balance
- **File**: `src/core/Game.ts:92`
- **Reproduction**: Play past floor 1 — never see Skeletons or Orcs
- **Expected**: Tougher enemies appear on deeper floors, scaling with currentFloor
- **Actual**: `ENEMY_TEMPLATES.slice(0, 3)` always returns [Rat, Bat, Goblin]. Skeleton and Orc definitions in enemies.ts are dead code
- **Suggestion**: Use `Math.min(ENEMY_TEMPLATES.length, 2 + currentFloor)` to expand pool per floor

### BUG-005: Large XP gains can skip level-ups — no multi-level-up loop
- **Severity**: major
- **Status**: open
- **Found**: 2026-07-21
- **Category**: gameplay
- **File**: `src/entities/Player.ts:24-33`
- **Reproduction**: Gain XP exceeding multiple level thresholds in one kill → only one level-up occurs
- **Expected**: All earned levels granted (e.g., 50 XP at level 1 → level 3)
- **Actual**: `gainXp()` checks for level-up exactly once with `if`, then returns. Remaining XP ignored
- **Suggestion**: Change `if` to `while` loop that rechecks `xpNeeded` after each level-up

### BUG-006: BSP room placement can overflow leaf boundary with small leaves
- **Severity**: major
- **Status**: open
- **Found**: 2026-07-21
- **Category**: gameplay
- **File**: `src/core/Dungeon.ts:59-73`
- **Reproduction**: Rare: aggressive BSP splits create leaf < MIN_ROOM_SIZE → `randomInt(MIN, max)` called with max < min → room exceeds bounds
- **Expected**: Room always fits within leaf and within MAP bounds
- **Actual**: No bounds guard on room carving. Room can exceed leaf boundary and potentially MAP dimensions
- **Suggestion**: Guard with `if (maxW < MIN_ROOM_SIZE || maxH < MIN_ROOM_SIZE) return;` and clamp room position

### BUG-007: Diagonal movement allows cutting through wall corners
- **Severity**: minor
- **Status**: open
- **Found**: 2026-07-21
- **Category**: gameplay
- **File**: `src/core/Game.ts:153`
- **Reproduction**: Stand at (0,0) with walls at (1,0) and (0,1); press diagonal key → move to (1,1)
- **Expected**: Diagonal blocked if either intermediate cardinal tile is a wall (roguelike convention)
- **Actual**: Only destination tile checked. Both intermediate tiles bypassed
- **Suggestion**: For diagonal moves, check `tiles[player.y][nx]` and `tiles[ny][player.x]` before allowing

### BUG-008: FOV uses TileType.Wall check instead of blocksVision property
- **Severity**: minor
- **Status**: open
- **Found**: 2026-07-21
- **Category**: rendering
- **File**: `src/systems/FOVSystem.ts:101`
- **Reproduction**: Currently no visible issue (walls always block vision). But future tile types with blocksVision=true but type≠Wall would render incorrectly
- **Expected**: FOV uses `TileData.blocksVision` property for transparency checks
- **Actual**: Hardcoded `tile.type === TileType.Wall` bypasses the interface abstraction
- **Suggestion**: Replace with `const blocks = this.tiles[y][x].blocksVision;`

### BUG-009: Save key binding shadowed if 's' added to KEY_BINDINGS
- **Severity**: minor
- **Status**: open
- **Found**: 2026-07-21
- **Category**: ui
- **File**: `src/systems/InputHandler.ts:30-60`
- **Reproduction**: If `s: {dx: 0, dy: 0}` added to KEY_BINDINGS, save handler at line 57 is never reached
- **Expected**: Save key works regardless of KEY_BINDINGS contents
- **Actual**: Generic KEY_BINDINGS check at line 30 fires first and returns, shadowing save handler
- **Suggestion**: Move save/pickup/wait checks above the generic KEY_BINDINGS check

### BUG-010: Game.state not initialized in constructor (non-null assertion risk)
- **Severity**: minor
- **Status**: open
- **Found**: 2026-07-21
- **Category**: crash
- **File**: `src/core/Game.ts:26`, `src/systems/InputHandler.ts:23`
- **Reproduction**: Currently safe (sync init), but refactoring could expose uninitialized state access
- **Expected**: State initialized in constructor or null-checked at access points
- **Actual**: `state!: GameState` declaration; set only in `start()`. No null guards at access sites
- **Suggestion**: Add guard check in InputHandler: `if (!this.game.state) return;`

### BUG-011: XP bar/display missing from HUD
- **Severity**: cosmetic
- **Status**: open
- **Found**: 2026-07-21
- **Category**: ui
- **File**: `src/ui/HUD.ts`, `index.html`
- **Reproduction**: Play game, gain XP — no visual indicator of XP progress toward next level
- **Expected**: XP bar or text showing current XP / XP needed for next level
- **Actual**: HUD shows Level but not XP progress. DOM has no XP element
- **Suggestion**: Add XP stat row in index.html and update in HUD.update()

### BUG-012: Items placed only in rooms 2-4, regardless of dungeon size
- **Severity**: cosmetic
- **Status**: open
- **Found**: 2026-07-21
- **Category**: balance
- **File**: `src/core/Game.ts:99`
- **Reproduction**: 8-room dungeon → items only in 3 rooms (2,3,4)
- **Expected**: Items distributed across more rooms in larger dungeons
- **Actual**: `rooms.slice(1, Math.min(rooms.length, 4))` limits to 3 rooms max
- **Suggestion**: Scale to `Math.min(rooms.length, 2 + Math.floor(rooms.length / 2))`

### BUG-013: Message log grows to 101 before trimming (off-by-one)
- **Severity**: cosmetic
- **Status**: open
- **Found**: 2026-07-21
- **Category**: ui
- **File**: `src/ui/HUD.ts:47`
- **Reproduction**: Generate 101+ messages → log holds 101 before one is removed
- **Expected**: Cap at exactly 100 messages
- **Actual**: While loop at line 47 runs AFTER appendChild at line 42. Temporary peak of 101
- **Suggestion**: Move while loop before append, or change condition to `> 99`

---

## Resolved Bugs

<!-- Fixed bugs are moved here, retaining history -->



*Last assessment: 2026-07-21 | 13 bugs found (3 critical, 3 major, 4 minor, 3 cosmetic)*
